# Raíz del servicio FDSN Event de USGS (sin /query ni querystring).
# La app le añade /query y /count según haga falta.
# También se acepta la forma antigua (.../fdsnws/event/1/query?format=geojson).
NEXT_PUBLIC_ENV_URL_API='https://earthquake.usgs.gov/fdsnws/event/1'
